from abc import ABC, abstractmethod
class Serializable(ABC):
    """
    A class meant to represent the concept of being serializable.
    In Python, serialization is typically handled by modules like 'pickle' or 'json',
    and this class serves as a conceptual placeholder.
    """
    pass
